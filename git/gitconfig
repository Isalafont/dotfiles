[color]
  branch = auto
  diff = auto
  interactive = auto
  status = auto
  ui = auto

[color "branch"]
  current = green
  remote = yellow

[core]
  pager = less -FRSX
	editor = nano

[alias]
  co = checkout
  st = status -sb
  br = branch
  ci = commit
  fo = fetch origin
  dif = !git --no-pager diff
  dt  = difftool
  stat = !git --no-pager diff --stat
  pf = push --force-with-lease


  # Clean merged branches
  swip = !git branch --merged master | grep -v 'master$' | xargs git branch -d && git remote prune origin

  sweep = !git branch --merged develop | grep -v 'develop$' | xargs git branch -d && git remote prune origin


  # http://www.jukie.net/bart/blog/pimping-out-git-log
  lg = log --graph --all --pretty=format:'%Cred%h%Creset - %s %Cgreen(%cr) %C(bold blue)%an%Creset %C(yellow)%d%Creset'

  # Serve local repo. http://coderwall.com/p/eybtga
  # Then other can access via `git clone git://#{YOUR_IP_ADDRESS}/
  serve = !git daemon --reuseaddr --verbose  --base-path=. --export-all ./.git

  m = checkout master
  main = checkout main
  dev = checkout develop

  # Removes a file from the index
  unstage = reset HEAD --

[help]
  autocorrect = 1

[push]
	default = simple

[branch "master"]
  mergeoptions = --no-edit

[pull]
  rebase = merges

[rebase]
  updateRefs = true

[init]
  defaultBranch = main
[user]
	email = isabelle.lafont@beta.gouv.fr
	name = Isabelle Lafont
[diff "ansible-vault"]
	textconv = ansible-vault view
